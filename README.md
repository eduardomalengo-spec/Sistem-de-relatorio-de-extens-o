# Sistem-de-relatorio-de-extens-o
Projeto que foi feito por varios ambientes para testes de refatoração e copilação, usando, hashmap + for(map.entry) + if/else + keySet(), values(), get, getKey(), getValue(), containsKey() e por ultimo size() para contabilizar quantos relatorios foram feitos
